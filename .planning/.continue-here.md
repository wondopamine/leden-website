---
context: research
phase: design-conformance
task: 5
total_tasks: 7
status: in_progress
last_updated: 2026-08-15T11:11:07.136Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through actual failures. Acknowledge each before resuming.

- [ ] CONSTRAINT: Serialize production builds and Playwright runs — parallel workers collided in the shared `.next` directory and produced a missing `BUILD_ID` failure — run build-backed suites one at a time.
- [ ] CONSTRAINT: Server Components must import link styling from `src/components/ui/button-variants.ts` — importing `buttonVariants` from the client-marked `ui/button.tsx` caused every storefront route to return 500 at runtime even though `next build` passed.
- [ ] CONSTRAINT: Google Fonts require network during `next build` — sandboxed builds fail fetching Fraunces/Inter — rerun the build or production Playwright with the already approved escalated network path.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Concurrent `.next` writers | Two agents ran production build/Playwright together; the shared build output became temporarily incomplete. | blocking | Before any build-backed command, confirm no other project build/Playwright is active; run the suite serially. |
| Client style export called from server | Server-rendered hero imported `buttonVariants` through the `"use client"` Button module and crashed at request time. | blocking | Server-rendered links import `@/components/ui/button-variants` directly; keep the Button re-export only for client consumers. |
| Treating build success as runtime proof | The client-boundary defect passed `next build` but failed on the first request. | advisory | Always follow build with real route rendering in Playwright. |

<current_state>

Branch: `codex/refactor-dx-design-conformance`.

The canonical contract, baseline tests, semantic tokens, primitives/gallery, storefront refactor, and admin control-tower refactor are committed. The current uncommitted working tree is the independent-review repair pass. It includes route metadata, 44px targets, language radio semantics, localized actionable order failures, semantic CardTitle levels, analytics error/retry, dirty-form guards, sticky tables, 320px admin coverage, third-font removal, reduced motion, and manifest updates.

The user interrupted a final `apply_patch` that would have converted additional link-like Buttons to true links and changed footer/order layouts to the declared 12-column grid. The patch did not apply. `git diff --check` was clean immediately afterward.

</current_state>

<completed_work>

Completed tasks and checkpoints:

- Canonical `DESIGN.md`, `.dx/design.json`, manifest, ticket, and plan — commit `8166dd5`.
- Baseline Playwright coverage — commit `edea7d5`.
- Semantic token/type layer — commit `0a4389a`.
- Shared primitives and responsive component gallery, independently reviewed — commit `fb7cd21`.
- Customer storefront refactor — commit `92fb0f6`.
- Admin operations refactor — commit `1c80afe`.
- First final independent review — verdict `fail`; all listed blockers now have implementations in the working tree.
- Customer repair verification: TypeScript/lint/DX clean; storefront Playwright 10/10 passed; no real order submission.
- Admin repair verification: TypeScript/lint/build/DX clean; production admin Playwright 3 passed and 2 safely gated skips; focused auth-free 320px preview 1 passed.
- Root integration after review: removed Geist Mono; added `button-variants.ts`; capped motion at 300ms; removed passive looping review/doodle motion; converted story proof from static card to editorial rule; moved hero/story to explicit 12-column layouts; sanitized admin error copy.

</completed_work>

<remaining_work>

1. Inspect `git status`, `git diff --check`, and the new `button-variants.ts` split before editing.
2. Finish the interrupted semantic-link cleanup. At minimum review every `nativeButton={false}` + `render={<Link|a>}` occurrence. Navigation should expose role `link`; style with the server-safe `buttonVariants` helper. Hero and sticky CTAs are already corrected. The order error menu link is already corrected.
3. Finish the declared-grid cleanup: convert customer footer and checkout two-column layouts to explicit 12-column spans. Re-evaluate other functional track templates rather than mechanically changing admin form grids.
4. Run combined `npx tsc --noEmit`, `npm run lint`, design JSON freshness, component manifest, token audit, static accessibility, TSX type scan, and resolved contrast.
5. Run `npx playwright test` serially with escalated network, then gallery Playwright. Capture fresh customer and admin evidence at 320/360/768/1280 or 1440. Do not submit orders or mutate production.
6. Spawn a fresh independent reviewer following `/Users/jeongwondo/.codex/dx-harness/agents/dx-design-review.md`. The builder must not grade its own work. Fix every blocker and re-review if necessary.
7. Write `docs/design-reviews/2026-08-15-dx-conformance.md`, update `docs/design-tickets/design-system-conformance.md`, check the final TODO review item, regenerate/check design JSON, and commit this DX review repair pass.
8. Resume the remaining LFG chain in order: read/run `ce-code-review mode:autofix plan:docs/plans/2026-08-15-001-refactor-dx-design-conformance-plan.md`; commit review fixes separately; read/run `ce-test-browser mode:pipeline`; read/run `ce-commit-push-pr`.

</remaining_work>

<decisions_made>

- Customer presentation is warm, local, and appetite-led; admin is calmer, denser, and operational.
- Forest is the primary action/status anchor; orange is supporting appetite/accent colour.
- Fraunces 500/600 and Inter 400/500/600 are the only UI typefaces. The TYP-1 L1 override is approved by Jeongwon Do.
- Public pages remain Server Components with narrow client interaction leaves.
- No schemas, pricing math, order payloads, auth rules, or successful server-action semantics may change during the visual refactor.
- Production mutations remain out of browser verification; fixture-gated non-production coverage is intentional.
- A true navigation destination must keep link semantics even when it visually uses Button styles.

</decisions_made>

<blockers>

- No active blocker. Seeded authenticated mutation coverage is optional and needs an explicit non-production Supabase target.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable position and next action.
2. This file — complete resume state and failure constraints.
3. `docs/plans/2026-08-15-001-refactor-dx-design-conformance-plan.md` — approved implementation and safety gates.
4. `DESIGN.md` and `.dx/component-manifest.json` — current design contract and component API record.
5. `/Users/jeongwondo/.codex/plugins/cache/compound-engineering-plugin/compound-engineering/3.1.0/skills/lfg/SKILL.md` — required remaining autonomous workflow.
6. `/Users/jeongwondo/.codex/dx-harness/agents/dx-design-review.md` — mandatory independent re-review format.

## Critical Anti-Patterns (do NOT repeat these)

- PARALLEL BUILD OUTPUT: never let two workers write `.next` concurrently; serialize build-backed verification.
- CLIENT EXPORT IN SERVER: never call `buttonVariants` from `ui/button.tsx` in a Server Component; import `ui/button-variants.ts` directly.
- BUILD-ONLY CONFIDENCE: always render the production routes after building.
- PRODUCTION MUTATION: never exercise live order/admin mutations without an explicit seeded non-production target.

## Infrastructure State

- No Café Le Den Next.js, build, or Playwright process was active at pause time.
- Browser artifacts remain gitignored.
- Final repair screenshots must be recaptured after cleanup; prior evidence exists in `/tmp/leden-final-*.png` and `/private/tmp/cafe-le-den-u6-fixed-*.png`.
- Network-restricted builds fail only while fetching the configured Google fonts; the approved escalated build path succeeds.

<context>

The design result is visually strong and the first reviewer agreed the storefront is distinctive and the admin reads as a credible café control tower. The review failed on semantics/state completeness, not visual direction. Those blockers are now implemented and scoped tests passed. The remaining work is integration discipline: finish semantic links and declared-grid cleanup, verify serially, obtain a clean independent verdict, record evidence, then complete the LFG review/browser/ship chain.

</context>

<next_action>

Start with: acknowledge the three blocking constraints, run `git status --short` and `git diff --check`, inspect `src/components/ui/button-variants.ts`, then complete the interrupted true-link and customer 12-column cleanup before any build.

</next_action>
