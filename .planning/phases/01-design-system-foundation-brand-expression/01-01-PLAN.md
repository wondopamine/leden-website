---
phase: 01-design-system-foundation-brand-expression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/brand/SPEC.md
requirements:
  - DSY-06
autonomous: true
tags:
  - brand
  - design-system
  - phase-1
must_haves:
  truths:
    - "A markdown file exists at .planning/brand/SPEC.md"
    - "It contains exactly 5 second-level sections in the order Typography, Photography, Voice, Motion, Mascot"
    - "Each of the 5 sections ends with a single line beginning '**Decision:**' that names a concrete, falsifiable choice"
    - "Typography Decision names a font face, weight, and size band that downstream tokens can implement"
    - "Motion Decision names duration values and easing curves that downstream motion tokens can implement"
    - "Each Decision line cites at least one PROJECT.md DNA reference (Graza, Monte Cafe, Craft, or Superr) and at least one inline image link"
  artifacts:
    - path: ".planning/brand/SPEC.md"
      provides: "Locked brand expression spec — Typography, Photography, Voice, Motion, Mascot decisions"
      contains: "**Decision:**"
  key_links:
    - from: ".planning/brand/SPEC.md (Typography Decision)"
      to: "Plan 02 globals.css typography tokens (--text-display/h1/h2/h3/body/caption/label)"
      via: "Decision line drives explicit size/weight/line-height values in PR2"
      pattern: "font|size|weight"
    - from: ".planning/brand/SPEC.md (Motion Decision)"
      to: "Plan 02 globals.css motion tokens (--duration-* / --ease-*)"
      via: "Decision line drives explicit ms values + easing curves in PR2"
      pattern: "duration|ease|cubic-bezier"
---

<objective>
Author and commit `.planning/brand/SPEC.md` — five named pillars (Typography, Photography, Voice, Motion, Mascot), each ending in a single `**Decision:**` line that downstream Phase 1 plans (typography tokens, motion tokens) and Phases 3/4 page rebuilds reference as locked brand intent.

Purpose: D-05 says brand spec lands FIRST. Typography Decision feeds typography token sizes/weights/line-heights in `globals.css`; Motion Decision feeds duration values and easing curves. Locking design intent before code prevents a token rewrite mid-phase.

Output: `.planning/brand/SPEC.md` — net-new file, planning artifact only (not runtime code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation-brand-expression/01-SPEC.md
@.planning/phases/01-design-system-foundation-brand-expression/01-CONTEXT.md
@.planning/phases/01-design-system-foundation-brand-expression/01-PATTERNS.md

<authoring_inputs>
Brand DNA references (locked in PROJECT.md):
- Graza — warm, playful, illustrative wordmark; saturated accents on cream backgrounds
- Monte Cafe — editorial cafe; serif display + photography-led layouts
- Craft — artisan, hand-feel, generous whitespace
- Superr — modern, clean, minimal grid

Logo anchors (locked in CONTEXT.md / SPEC.md):
- cream `#EFE7D2`
- forest green `#2F5436`
- warm orange `#D9682E`
- watermelon mascot (existing logo asset)

D-04: Claude synthesizes the spec; user reviews and edits each `**Decision:**` line in place. Not delegated to /design-consultation.
D-06: Each Decision line cites at least one DNA reference (Graza/Monte Cafe/Craft/Superr) plus one inline image link per pillar. Evidence-based and short — no free-form rationale paragraphs.
</authoring_inputs>

<acceptance_greps>
From SPEC.md REQ 6 and PATTERNS.md verification block:

```bash
grep -E "^## (Typography|Photography|Voice|Motion|Mascot)$" .planning/brand/SPEC.md | wc -l   # must be 5
grep -cE "^\*\*Decision:\*\*" .planning/brand/SPEC.md                                          # must be 5
```
</acceptance_greps>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .planning/brand/SPEC.md with 5 pillars × 1 Decision line each</name>

  <read_first>
    - .planning/PROJECT.md (DNA references, watermelon mascot, key decisions)
    - .planning/phases/01-design-system-foundation-brand-expression/01-SPEC.md (REQ 6 — exact structure: 5 `##` sections in order Typography, Photography, Voice, Motion, Mascot, each ending in `**Decision:**` line)
    - .planning/phases/01-design-system-foundation-brand-expression/01-CONTEXT.md (D-04, D-05, D-06 — author/structure rules)
    - .planning/phases/01-design-system-foundation-brand-expression/01-PATTERNS.md (skeleton at lines 48–69 of `.planning/brand/SPEC.md`)
  </read_first>

  <files>.planning/brand/SPEC.md</files>

  <action>
Create the directory `.planning/brand/` if it does not exist, then write `.planning/brand/SPEC.md` matching the structure below exactly. The file MUST contain — in this order — exactly 5 second-level (`##`) sections: Typography, Photography, Voice, Motion, Mascot. Each section MUST end with a single line that begins at column 0 and matches the literal regex `^\*\*Decision:\*\*`. Do NOT use `###` for the Decision line. Do NOT add any other `## ` sections.

Per D-06, each section MUST cite at least one DNA reference (Graza, Monte Cafe, Craft, or Superr by name) and include at least one inline image link in markdown form `![alt](https://...)` or `[label](https://...)` pointing at a public reference image. Use established brand homepages for the image links: `https://graza.co`, `https://montecafe.com.au`, `https://craft-design.co`, `https://www.superr.studio` (or a specific image URL the maintainer can hot-swap later — use a publicly reachable URL in the initial draft so the file commits cleanly).

Per D-08 mindset (single source of truth), each Decision line MUST be a single one-liner with concrete values (no hedge words like "around", "roughly", "TBD"). Examples:
- Typography — `**Decision:** Display face — "Editorial New" serif, weight 500, 56–64px; body — "Inter" sans, weight 400, 16px / 1.5; caption — Inter weight 500, 13px / 1.4.`
- Motion — `**Decision:** Durations — fast 150ms, base 300ms, slow 500ms; eases — in cubic-bezier(0.4,0,1,1), out cubic-bezier(0,0,0.2,1), spring cubic-bezier(0.34,1.56,0.64,1).`

Required structure (skeleton — fill in concrete decisions; do NOT keep angle-bracket placeholders in the committed file):

```markdown
# Cafe Le Den — Brand Expression Spec

**Locked:** 2026-04-29
**Consumed by:** Phase 1 tokens (Typography Decision → `--text-*`; Motion Decision → `--duration-*` / `--ease-*` in `src/app/globals.css`); Phases 3 + 4 page rebuilds reference all 5 Decisions as locked brand intent.

## Typography
DNA: Monte Cafe editorial display + Superr clean body. ![Monte Cafe sample](https://montecafe.com.au)
Concrete pairing — display serif for hero/h1; body sans for paragraph + UI; caption sans for labels and meta.
**Decision:** Display — "Editorial New" serif, weight 500, 56–64px / 1.05; H1–H3 — Editorial New 40 / 32 / 24px, weights 500/500/600; body — Inter sans 16px / 1.5; caption — Inter 13px / 1.4 weight 500; label — Inter 12px / 1.3 weight 600 uppercase tracking 0.05em.

## Photography
DNA: Graza warm/playful + Monte Cafe editorial. ![Graza imagery](https://graza.co)
Concrete style — warm-tone product + ambient cafe shots, natural light, mid-shadow, food-forward; no stock smiles, no Unsplash filler.
**Decision:** Warm-tone product photography on cream/forest backdrops; natural daylight, mid-shadow, in-store ambient; aspect 4:5 portrait or 16:9 landscape; no human-face hero shots in v1.

## Voice
DNA: Graza warm/playful + Craft artisan. ![Craft sample](https://craft-design.co)
Concrete tone — first-person plural, short sentences, food-forward nouns, light humour without slang.
**Decision:** Friendly-direct, first-person plural ("we"), 8–14 word sentences, food-forward nouns, no exclamation marks, EN/FR parity (FR copy ≤ 1.15× EN length).

## Motion
DNA: Superr clean + Craft restraint. ![Superr motion sample](https://www.superr.studio)
Concrete budget — three durations, three easings; staggered hero fade-ups; no parallax, no scroll-jacking.
**Decision:** Durations — fast 150ms, base 300ms, slow 500ms; eases — in cubic-bezier(0.4,0,1,1), out cubic-bezier(0,0,0.2,1), spring cubic-bezier(0.34,1.56,0.64,1); hero fade-up uses slow + out; respects `prefers-reduced-motion`.

## Mascot
DNA: Graza playful illustrative mark. ![Watermelon mascot context](https://graza.co)
Concrete usage — watermelon mascot appears as a small accent (≤ 64px) in hero, footer, and confirmation; never as a primary CTA, never animated > 500ms.
**Decision:** Watermelon mascot — accent role only, max 64px square, allowed in hero corner / footer / confirmation success state; never inside buttons, never as logo replacement, never animated longer than `--duration-slow`.
```

After writing, verify both grep counts return exactly `5`. If either count is wrong, fix the headings or Decision lines until both pass. Do NOT add allowlists, hedge words, or extra sections — D-04 says the user will review and edit Decision lines in place; the file committed here is the editable starting point.
  </action>

  <verify>
    <automated>cd /Users/jeongwondo/Developer/leden-website &amp;&amp; SECTIONS=$(grep -cE "^## (Typography|Photography|Voice|Motion|Mascot)$" .planning/brand/SPEC.md) &amp;&amp; DECISIONS=$(grep -cE "^\*\*Decision:\*\*" .planning/brand/SPEC.md) &amp;&amp; test "$SECTIONS" -eq 5 &amp;&amp; test "$DECISIONS" -eq 5 &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/brand/SPEC.md` exits 0
    - Section count: `grep -cE "^## (Typography|Photography|Voice|Motion|Mascot)$" .planning/brand/SPEC.md` returns exactly `5`
    - Decision count: `grep -cE "^\*\*Decision:\*\*" .planning/brand/SPEC.md` returns exactly `5`
    - Order check: `grep -nE "^## (Typography|Photography|Voice|Motion|Mascot)$" .planning/brand/SPEC.md` lists Typography first, Mascot last (line numbers monotonically increasing in that named order)
    - DNA citation: `grep -cE "Graza|Monte Cafe|Craft|Superr" .planning/brand/SPEC.md` returns at least `5` (one per pillar)
    - Inline image link per pillar: `grep -cE "\]\(https?://" .planning/brand/SPEC.md` returns at least `5`
    - Typography Decision specifies a numeric size — `grep -E "^\*\*Decision:\*\*" .planning/brand/SPEC.md | head -1 | grep -E "[0-9]+px"` returns a match
    - Motion Decision specifies ms — `grep -E "^\*\*Decision:\*\*" .planning/brand/SPEC.md | sed -n '4p' | grep -E "[0-9]+ms"` returns a match
  </acceptance_criteria>

  <done>
.planning/brand/SPEC.md exists, all 6 grep checks above pass, file is committed in this PR. Downstream Plan 02 can read concrete Typography sizes/weights and Motion durations/easings from this file when populating tokens.
  </done>
</task>

</tasks>

<verification>
Run all acceptance greps from SPEC.md REQ 6 and PATTERNS.md against the post-task tree:

```bash
grep -E "^## (Typography|Photography|Voice|Motion|Mascot)$" .planning/brand/SPEC.md | wc -l   # 5
grep -cE "^\*\*Decision:\*\*" .planning/brand/SPEC.md                                          # 5
grep -cE "Graza|Monte Cafe|Craft|Superr" .planning/brand/SPEC.md                               # ≥ 5
grep -cE "\]\(https?://" .planning/brand/SPEC.md                                              # ≥ 5
```

All four MUST pass before this PR ships. No npm/lint impact (planning artifact only).
</verification>

<success_criteria>
- [ ] `.planning/brand/SPEC.md` committed to git on the phase-1 branch
- [ ] Exactly 5 `##` pillar sections in correct order
- [ ] Exactly 5 `**Decision:**` lines, one per pillar
- [ ] Each Decision is a single concrete one-liner with measurable values (sizes, weights, ms, easing curves)
- [ ] Each pillar cites a DNA reference and includes an inline image link
- [ ] Typography Decision is consumable as input to `--text-*` tokens in Plan 02
- [ ] Motion Decision is consumable as input to `--duration-*` / `--ease-*` tokens in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation-brand-expression/01-01-SUMMARY.md` per the GSD summary template — record the 5 Decision one-liners verbatim so Plan 02 can quote them directly when emitting token values.
</output>
